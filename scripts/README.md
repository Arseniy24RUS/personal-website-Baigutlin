# Collection and validation scripts

Adapted from Arseniy24RUS/personal-website at d5a18a6. The upstream code license is retained in [UPSTREAM_LICENSE.txt](../docs/UPSTREAM_LICENSE.txt). This does not relicense Baigutlin's photographs, writing, diploma scans or third-party assets.

See [operations](../docs/REFRESH_OPERATIONS.md), [credentials](../docs/GITHUB_SECRETS_SETUP.md) and [pipeline](../docs/PIPELINE_SPEC.md). Run `python -m unittest discover -s tests/unit -v`, `npm run seo:check` and `npm run test:e2e` before publishing code. Browser automation dependencies are installed by the workflow.
