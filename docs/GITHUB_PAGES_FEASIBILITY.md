# Static hosting

GitHub Pages serves the RU/EN HTML, JSON, CSS, JavaScript and local images. Python/browser collection runs in GitHub Actions, not in the visitor's browser. A refresh requests a Pages build after successful publication, then compares deployed output with the validated local files. The canonical domain remains `baigutlin.ru`.
