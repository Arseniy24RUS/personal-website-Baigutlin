# eLibrary refresh

The authoritative target is AuthorID **1170779**, SPIN **6676-3700** in `config/profile.yml`. The browser collector loads the author profile and complete publication listing through the authorized home VPN, reuses a verified encrypted session or performs ordinary login, and records metrics/publications/citations independently.

Partial or blocked responses preserve the previous complete observations and all published records. Bibliographic enrichment retains reviewed references and translations. Interactive challenges are recorded as unavailable rather than bypassed or treated as empty results. Use the `elibrary` source selector for a focused manual run. See [operations](REFRESH_OPERATIONS.md) and [credentials](GITHUB_SECRETS_SETUP.md).
